{
  "name": "istanbul-stdin-reporter",
  "version": "0.0.1",
  "description": "Create Istanbul code coverage reports by passing coverage data through STDIN",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "istanbul-stdin-reporter": "index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/miscer/istanbul-stdin-reporter.git"
  },
  "keywords": [
    "istanbul",
    "coverage",
    "reporter",
    "collector",
    "stdin"
  ],
  "author": "Michal Miskernik <michal@miskernik.sk>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/miscer/istanbul-stdin-reporter/issues"
  },
  "homepage": "https://github.com/miscer/istanbul-stdin-reporter#readme",
  "dependencies": {
    "istanbul": "^0.4.5",
    "yargs": "^6.3.0"
  }
}
